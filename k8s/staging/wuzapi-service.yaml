apiVersion: v1
kind: Service
metadata:
  name: wuzapi
  namespace: wuzapi-staging
  labels:
    app: wuzapi
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: wuzapi
